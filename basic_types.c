#include <stdio.h>
int main(){
    int max_recursive_calls = 100;
    char mode = 'S';
    float throttle_speed = 0.9;
    printf("Max recursive calls: %d\n", max_recursive_calls);
  printf("IO mode: %c\n", mode);
  printf("Throttle speed: %f\n", throttle_speed);
  return 0;
}


